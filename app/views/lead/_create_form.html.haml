%h2 Create a new group
%p
  Please choose a unique name and a password for your group.
  #new-group-errors.notice
= form_for Group.new, remote: true, url: create_lead_path, format: :json do |f|
  %table
    %tr
      %td
        = f.label :groupname
      %td
        = f.text_field :groupname, maxlength: 20, required: true, size: 20
    %tr
      %td
        = f.label :password
      %td
        = f.password_field :password, maxlength: 20, required: true, size: 20
    %tr
      %td(colspan=2)
        = f.submit'Create group', class: 'submit'
%p
  Careful to keep your password secret from your students ;)
